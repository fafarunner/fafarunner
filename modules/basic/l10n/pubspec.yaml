name: l10n
description: l10n module.
publish_to: none

environment:
  sdk: '>=3.9.0 <4.0.0'

resolution: workspace

# Add regular dependencies here.
dependencies:
  slang_flutter: ^4.8.0

dev_dependencies:
  build_runner: ^2.5.4
  slang_build_runner: ^4.8.1
  test: ^1.25.15
  very_good_analysis: ^10.0.0
