name: logger
description: logger module.
publish_to: none

environment:
  sdk: ^3.8.0

resolution: workspace

# Add regular dependencies here.
dependencies:
  logging: ^1.3.0

dev_dependencies:
  test: ^1.25.15
  very_good_analysis: ^9.0.0
